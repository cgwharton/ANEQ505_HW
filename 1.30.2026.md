Friday Reservations: 
```
sinteractive --reservation=aneq505 --time=01:00:00 --partition=amilan --nodes=1 --ntasks=2 --qos=normal
```
Any other time 
```
ainteractive --
```
Working Directory
```

`# replace "username" with YOUR CSU EID   cd /scratch/alpine/username@colostate.edu`  
  
`# make a directory using the mkdir command   mkdir decomp_tutorial`  
  
`# move into that directory using cd   cd decomp_tutorial`
```

